# AGENTS.md Creation Session

Create a project-specific `AGENTS.md` at `{{PROJECT_PATH}}/AGENTS.md`. Do not modify the source fragments or create or edit any other files unless the user explicitly asks.

First inspect the project and briefly interview the user about the languages, frameworks, tools, workflows, and team conventions that apply. Ask only the questions needed to select relevant guidance.

The selected skills source is rooted at `{{SOURCE_ROOT}}`. Its available guidance fragments are:

```text
{{FRAGMENT_PATHS}}
```

Read the relevant fragments from that source. Treat them as reference material: select applicable concrete rules, reconcile redundant or conflicting advice, and do not blindly concatenate every fragment. Do not invent conventions not supported by the project, the user's answers, or these fragments.

Write one concise, actionable `AGENTS.md` for coding agents. Before finishing, verify it is non-empty, project-specific, and contains no duplicated or contradictory instructions.
